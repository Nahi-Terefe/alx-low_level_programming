#include "main.h"

/**
 * mul - do multiplication of two number.
 * @a: accepts any int value.
 * @b: accepts any int value.
 * Return: multiplication result.
 */
int mul(int a, int b)
{
	return (a * b);
}
